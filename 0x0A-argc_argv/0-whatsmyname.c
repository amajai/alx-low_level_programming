#include <stdio.h>

/**
 * main - prints its name.
 * @argc : args counts.
 * @argv : args values in array.
 *
 * Return: Always 0.
 */
int main(__attribute__((unused))int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}

